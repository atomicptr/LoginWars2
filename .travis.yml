language: node_js
node_js:
    - "0.12"
before_script:
    - npm install -g gulp
    - npm install -g bower
    - npm install
    - bower install
    - sudo apt-get install wine
script: gulp build
after_success: gulp build-installer
